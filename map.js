// // map filter reduce 
// Map
// The map() method is used for creating a new array from an existing one, applying a function to each one of the elements of the first array.



const arr = [1, 2, 3, 4, 5, 6];

// const arrList = arr.map((item) => (item * 5));
// console.log(arr);
// console.log(arrList);




const aList = arr.forEach((item,index)=>(item*5))
console.log(arr)
console.log(aList)






